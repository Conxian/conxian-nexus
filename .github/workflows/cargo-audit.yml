name: Cargo Audit

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 8 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@7152abe8f0a256eed82b308eb9939007b031686a # stable

      - name: Install cargo-audit
        run: cargo install cargo-audit --locked

      - name: Generate lockfile and audit
        run: cargo generate-lockfile && cargo audit
