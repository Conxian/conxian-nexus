[extend]
useDefault = true

# Public, deterministic BN254 verification-key identifier generated by
# examples/generate_bitvm_groth16_fixture.rs for the test-only fixture. Keep
# generic-api-key enabled; allow only this exact non-secret fixture value.
[allowlist]
description = "Deterministic public BitVM Groth16 v1 test verification-key identifier"
regexTarget = "secret"
regexes = [
  '''^24ec0adf5c2843f5f255c7f7ded7a6f91f37c23d10c8fb1e5613d438fa895b55$''',
]
